<% provide :title, @user.name %>
<div id="users">
  <div class="row table table-striped text-center">
    <div class="col-sm-4 col-xs-4">
      <%= @user.name %>
    </div>
    <div class="col-sm-4 col-xs-4">
      <%= @user.email %>
    </div>
    <div class="col-sm-4 col-xs-4">
      <%= @user.dogs.any? ? @user.dogs.size : 0 %>
      <%= t "users.image" %>
    </div>
  </div>
  <hr />
  <%= render @user.dogs %>
</div>
